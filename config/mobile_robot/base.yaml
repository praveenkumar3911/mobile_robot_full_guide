base:
  mass: 1.0           # Base mass in Kilograms
    x_size: 0.3         # Base length in meters (x-axis)
      y_size: 0.15        # Base width in meters (y-axis)
        z_size: 0.05        # Base height in meters (z-axis)
          mesh: ''            # Name of the mesh files. Leave it empty ('') to have a white box base. 
          
